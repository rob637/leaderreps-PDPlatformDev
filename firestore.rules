// Firestore rules
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;                // everyone can read (adjust as needed)
      allow write: if request.auth != null; // only authenticated (anon counts)
    }
  }
}